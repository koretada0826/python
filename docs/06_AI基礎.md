# Day 6:AI連携【基礎】 - Claude/OpenAI API

## 💡 これは何?(小学生でもわかる説明)

****ChatGPT/Claude を自分のアプリから動かす**(API)**

### なぜ大事?
「AI使えます」と言える人は単価が3倍になる

### イメージ
**頭のいい部下**を月3000円で雇って24時間働かせる

### Claude Code に何を頼めばいいか
この章で覚えるのは「**何ができるか**」と「**何を頼めばいいか**」だけでOK。
具体的なコードは Claude Code に書かせる。

---

## 💰 この章後の月収:**10〜30万円**(高単価圏)
## ⏱ 所要:3時間

ここを使えると単価が**5-10倍**になる。
次のDay 7「AI高度」で**月100万圏**へ。

---

## ゴール

- Claude API 使える
- AIに要約・分析させられる
- Streamlit でWebアプリ化
- **AIチャットボットを成果物として完成**

---

## 1. APIキー取得(20分)

### Anthropic(Claude)

1. https://console.anthropic.com 登録
2. 「API Keys」→ 新規発行
3. クレジットカード登録、$5チャージで充分

### .env でキー安全管理

`.env` ファイル(プロジェクトに置く):
```
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
```

`.gitignore` に追加(必須!):
```
.env
__pycache__/
*.pyc
.DS_Store
```

→ **.envをGitHubに上げると悪用される**。絶対 .gitignore する。

---

## 2. Claude API 基本(20分)

```
pip install anthropic python-dotenv
```

```python
import os
from anthropic import Anthropic
from dotenv import load_dotenv

load_dotenv()
client = Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

response = client.messages.create(
    model="claude-opus-4-7",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "こんにちは!"}
    ]
)

print(response.content[0].text)
```

→ Claudeから応答が返ってくる。

---

## 3. 実用例3つ(40分)

### 例1:文章要約

```python
def 要約(長文, 文字数=200):
    response = client.messages.create(
        model="claude-opus-4-7",
        max_tokens=500,
        messages=[
            {"role": "user", "content": f"以下を{文字数}文字以内で要約:\n\n{長文}"}
        ]
    )
    return response.content[0].text
```

### 例2:メール下書き

```python
def メール下書き(用件, 相手):
    prompt = f"""
ビジネスメールを書いて。
用件: {用件}
相手: {相手}
トーン: 丁寧
"""
    r = client.messages.create(
        model="claude-opus-4-7",
        max_tokens=1024,
        messages=[{"role": "user", "content": prompt}]
    )
    return r.content[0].text
```

### 例3:データ分析コメント

```python
import pandas as pd

df = pd.read_csv("sales.csv")
summary = df.describe().to_string()

prompt = f"""
売上データの統計から、ビジネス的な気づきを3つ箇条書きで:
{summary}
"""

r = client.messages.create(...)
print(r.content[0].text)
```

→ **データ → AI で分析 → レポート**自動化。

---

## 4. OpenAI も同様(15分)

```
pip install openai
```

```python
from openai import OpenAI
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

r = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "こんにちは"}]
)
print(r.choices[0].message.content)
```

---

## 5. Streamlit でWebアプリ化(40分)

「**Pythonだけで Webアプリ作れる**」ライブラリ。

```
pip install streamlit
```

### `app.py`

```python
import streamlit as st
import os
from anthropic import Anthropic
from dotenv import load_dotenv

load_dotenv()
client = Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

st.title("AIチャットボット")

if "messages" not in st.session_state:
    st.session_state.messages = []

# 過去メッセージ
for msg in st.session_state.messages:
    with st.chat_message(msg["role"]):
        st.write(msg["content"])

# 入力
if prompt := st.chat_input("質問してください"):
    st.session_state.messages.append({"role": "user", "content": prompt})
    with st.chat_message("user"):
        st.write(prompt)
    
    r = client.messages.create(
        model="claude-opus-4-7",
        max_tokens=1024,
        messages=st.session_state.messages
    )
    answer = r.content[0].text
    
    st.session_state.messages.append({"role": "assistant", "content": answer})
    with st.chat_message("assistant"):
        st.write(answer)
```

### 起動

```
streamlit run app.py
```

→ ブラウザが開いてチャットボット動く!

---

## 6. RAG(自社文書検索Bot)— 概念だけ(15分)

「**社内のPDFを学習させて質問に答えるBot**」。

```
PDF → 細かく分割 → ベクトル化 → DBに保存
   ↓
質問 → ベクトル化 → 関連文書検索 → AIに渡す → 回答
```

法律事務所・士業から**月50万円〜の高単価案件**が取れる。

```
pip install langchain langchain-anthropic chromadb pypdf
```

→ クロードに「LangChainでRAGの最小例書いて」と頼む。**動かしながら学べばOK**。

---

## 7. ⭐ 売れる成果物 #4:AIアシスタント(40分)

### 仕様

- Streamlit でWebチャットUI
- Claude API 使用
- 「秘書モード」「翻訳モード」「要約モード」が切替可能
- 履歴保存

### クロードに頼む

```
Streamlit でWebチャットボット作って。

機能:
- サイドバーで3つのモード切替: 秘書 / 翻訳 / 要約
- モード別に system prompt を変える
  - 秘書: 「あなたは丁寧な秘書」
  - 翻訳: 「日英・英日翻訳」
  - 要約: 「文章を3行で要約」
- 履歴を session_state に保持
- Claude Opus 4.7 使用

.env から ANTHROPIC_API_KEY 読む。
```

→ **これだけでランサーズで5-10万円**。

---

## 8. コスト管理(参考)

| モデル | 入力料金 / 1M tokens | 出力料金 |
|---|---:|---:|
| Claude Opus 4.7 | $15 | $75 |
| Claude Sonnet 4.6 | $3 | $15 |
| Claude Haiku 4.5 | $0.25 | $1.25 |
| GPT-4o | $2.5 | $10 |

→ 個人テストは月$5-20で充分。大量処理なら Haiku を使う。

---

## ✅ チェック

- [ ] APIキー取得・.env 安全管理
- [ ] Claude API でハロー応答できる
- [ ] AIに要約・メール下書きさせられる
- [ ] Streamlit でWebアプリ動かせる
- [ ] RAG の概念理解(完成は後で)
- [ ] **売れる成果物#4(AIチャットボット)動いた**

---

## 進捗

```
Day 6: AI連携完了。Streamlit でAIチャットボット完成。ポートフォリオ3作品目。
```

## 次

`07_GitHub.md` へ。**作品集を世界に公開して信用UP**。
