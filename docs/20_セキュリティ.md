# Day 20:セキュリティ - 法人案件で必ず聞かれる

## 💡 これは何?(小学生でもわかる説明)

**ハッカーから家(=自分が作ったサービス)を守るための鍵・警報・金庫の付け方を学ぶ日**

### なぜ大事?
法人案件は「**セキュリティ大丈夫?**」で必ず聞かれる。
ここで答えられないと、技術力どんなに高くても**契約破棄**。
逆に、最低限のセキュリティ常識があれば**月+10〜30万円**。

### イメージ
- **OWASP Top 10**=「**家にやって来る泥棒の手口10選**」
- **bcrypt**=「**金庫**」(パスワードを誰にも読めない形にする)
- **SQLインジェクション対策**=「**玄関の鍵**」(プレースホルダで防ぐ)
- **XSS対策**=「**マスク**」(他人のブラウザに変な薬を流させない)
- **JWT + .env**=「**入店パス + 隠し金庫**」
- **HTTPS**=「**通話の暗号化**」(盗聴されても解読できない)
- **レート制限**=「**入店人数制限**」(1秒に100人来たら門を閉める)

### Claude Code に何を頼めばいいか
セキュリティ監査・脆弱性診断・暗号化実装・bcrypt設定は全部クロード。
あなたは「**何を守るか**」を伝えるだけ。

---

## 💰 この章後の月収:**+10〜30万円**(法人受注率UP)
## ⏱ 所要:2-3時間

「**セキュリティ大丈夫?**」に答えられないと法人で失格。

---

## ゴール

- OWASP Top 10 を理解
- パスワードハッシュ・JWT 安全運用
- SQLインジェクション・XSS 防御
- 機密情報の管理
- 法人案件でセキュリティ質問に答えられる

---

## 1. OWASP Top 10(=泥棒の手口10選)(20分)

### 1-1. なぜ知る必要?

> **世界で「最重要」とされる10大脆弱性**。
> ここを押さえれば**90%の攻撃を防げる**。
> 逆に、ここを知らずにサービス公開すると**1日で乗っ取られる**。

| # | 脆弱性 | 例 |
|---|---|---|
| 1 | **アクセス制御の不備** | 他人のデータが見れる |
| 2 | **暗号化の失敗** | パスワード平文保存 |
| 3 | **インジェクション** | SQLi, XSS |
| 4 | **設計上の欠陥** | 機密情報をURLに |
| 5 | **設定ミス** | デバッグモード本番ON |
| 6 | **古い依存ライブラリ** | 脆弱性のあるpip |
| 7 | **認証の失敗** | 弱いパスワード許可 |
| 8 | **データ整合性** | 改ざん検知なし |
| 9 | **ログ・監視不足** | 攻撃気づけない |
| 10 | **SSRF** | サーバーから内部攻撃 |

→ **これ知ってるだけで法人面談突破**。

---

## 2. パスワード保護(=金庫に入れる)(15分)

### 2-1. NG(絶対やるな)

```python
# ❌ 平文保存
user.password = "tarou123"

# ❌ MD5 / SHA1(=10年前の弱い暗号、辞書攻撃で即解読)
import hashlib
user.password = hashlib.md5(b"tarou123").hexdigest()
```

### 2-2. OK

```python
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=["bcrypt"])

# ハッシュ化(保存時)
user.password_hash = pwd_context.hash("tarou123")

# 検証(ログイン時)
if pwd_context.verify("tarou123", user.password_hash):
    print("OK")
```

→ **bcrypt or argon2**を使う。MD5/SHA1 は禁止。

> **なぜbcryptが安全?**
> - **ソルト自動生成**(=同じパスでも毎回違うハッシュ)
> - **計算が遅い**(=総当たり攻撃が現実時間で無理)
> - **GPU耐性**(=スパコンでも100年かかる)

### よくある間違い

| ミス | 何が起こる | 直し方 |
|---|---|---|
| 平文保存 | DB漏洩で全パス露出 | 必ずbcrypt |
| MD5/SHA1 | 数秒で解読 | bcrypt or argon2 |
| 自作ハッシュ | 必ず脆弱性あり | 標準ライブラリ使う |

---

## 3. SQLインジェクション(=玄関の鍵)(20分)

### 3-1. 危険コード

```python
# ❌ 文字列埋め込み(SQLi 食らう)
cursor.execute(f"SELECT * FROM users WHERE name = '{name}'")
```

→ ユーザーが `' OR 1=1; --` と入れたら**全データ奪われる**。

> **解読:**
> `WHERE name = '' OR 1=1; --'`
> → `1=1` は常に真 → **全件マッチ** → DB全データ流出。

### 3-2. 安全コード

```python
# ✅ プレースホルダ
cursor.execute("SELECT * FROM users WHERE name = ?", (name,))

# SQLAlchemy ORM
db.query(User).filter(User.name == name).all()

# SQLAlchemy text + params
db.execute(text("SELECT * FROM users WHERE name = :name"), {"name": name})
```

→ **絶対に文字列連結しない**。プレースホルダ必須。

> **プレースホルダの仕組み:**
> 値とSQL文を**別々に**DBに送る。
> 値の中にSQL文が混じってても、**ただの文字列として扱う**。
> = 攻撃が無効化される。

---

## 4. XSS(クロスサイトスクリプティング)(=マスク)(15分)

### 4-1. 何が起こる?

「**他人のブラウザで悪意あるJSを実行**」される攻撃。

### 4-2. 危険(Webアプリ)

```python
# ❌ ユーザー入力をそのままHTML出力
return f"<h1>こんにちは {name}</h1>"
# → name に <script>alert(1)</script> 入ると実行される
```

### 4-3. 安全

```python
# ✅ Jinja2 テンプレート(自動エスケープ)
return render_template("hello.html", name=name)

# Streamlit/FastAPI の通常使用は安全(JSON返すだけ)
```

### 4-4. markdown を扱う時の罠

```python
# ❌ Markdown → HTML 変換時の生HTML許可
markdown.markdown(text, extensions=["html"])

# ✅ 生HTML 禁止 + サニタイズ
import bleach
clean = bleach.clean(html, tags=["p", "b", "i"], strip=True)
```

> **エスケープとは:**
> `<` を `&lt;`、`>` を `&gt;` に変換 → ブラウザがHTMLとして解釈しない → JS実行されない。

---

## 5. CSRF(クロスサイトリクエストフォージェリ)(=なりすまし防止)(10分)

### 5-1. 何が起こる?

「他人のサイトから自分のサイトに**勝手なリクエスト**送られる」。

> **例:** ユーザーが銀行サイトにログイン中、悪意あるサイトを開く →
> 悪意サイトが「100万円送金」のリクエストを銀行に送る → **送金完了**。

### 5-2. 防御

- POST/PUT/DELETE は **CSRFトークン**で保護
- FastAPI なら `fastapi-csrf-protect` 使う
- API なら **JWT + Authorization ヘッダー**で十分

---

## 6. 機密情報の管理(=隠し金庫)(20分)

### 6-1. NG

```python
# ❌ ハードコード
API_KEY = "sk-xxxxxxxxxxxx"

# ❌ Git にコミット
```

### 6-2. OK

```python
# ✅ .env ファイル
import os
from dotenv import load_dotenv
load_dotenv()
API_KEY = os.getenv("API_KEY")
```

`.gitignore` に必ず:
```
.env
.env.local
*.pem
secrets/
credentials.json
```

### 6-3. 本番(クラウド)

| サービス | 機密管理 |
|---|---|
| AWS | Secrets Manager / Parameter Store |
| Render | 環境変数(UI入力) |
| Heroku | Config Vars |

→ **環境変数経由**が世界標準。

### 6-4. Git に上げてしまった時(=即対応)

```bash
# 履歴から完全削除(BFG Repo-Cleaner)
brew install bfg
bfg --replace-text passwords.txt

# 古いキーは即無効化(Anthropic/OpenAI管理画面で)
```

→ **見つけたら即無効化**。流出キーは数分で悪用される。

> **怖い実例:** OpenAI APIキーをGitに上げて30秒で発見・悪用 → $5,000の請求。
> **キー無効化を最優先**。履歴削除は後でいい。

---

## 7. 認可(=身分証だけでなく権限の確認)(15分)

### 7-1. 認証 vs 認可

| 用語 | 意味 | 比喩 |
|---|---|---|
| **認証(Authentication)** | あなたは誰? | 入店時の身分証チェック |
| **認可(Authorization)** | 何できる? | VIPルームに入れるか確認 |

### 7-2. 基本

```python
@app.get("/admin")
def admin_panel(user = Depends(get_current_user)):
    if not user.is_admin:
        raise HTTPException(403, "管理者のみ")
    return {...}

@app.delete("/users/{id}")
def delete_user(id: int, user = Depends(get_current_user)):
    if user.id != id and not user.is_admin:
        raise HTTPException(403, "他人のアカウント削除禁止")
    ...
```

→ **「自分のデータ以外見れない」**を必ず実装。

> **OWASP #1の脆弱性:** ログインしてれば**他人のデータも見れちゃう**バグ。
> 数行のチェック忘れで個人情報全流出 → ニュースになる。

---

## 8. レート制限(=入店人数制限)(15分)

### 8-1. なぜ?

「**1秒に100回叩かれる**」攻撃から守る。

```bash
pip install slowapi
```

```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter

@app.get("/api/data")
@limiter.limit("10/minute")
def get_data(request: Request):
    return {...}
```

→ 1分10回まで。超過は 429 Too Many Requests。

> **守れる攻撃:**
> - DoS攻撃(=サービス停止狙い)
> - ブルートフォース(=パスワード総当たり)
> - スクレイピング(=データ大量取得)

---

## 9. HTTPS(=通話の暗号化)(15分)

### 9-1. なぜ必須?

- 本番は**必ず HTTPS**
- HTTP は盗聴される(=Wi-Fiカフェで丸見え)

### 9-2. 取得方法

| 方法 | 価格 |
|---|---|
| Let's Encrypt | 無料 |
| Cloudflare | 無料(DNS経由) |
| AWS ACM | 無料 |

### 9-3. 設定

```python
# FastAPI のリダイレクト
from fastapi.middleware.httpsredirect import HTTPSRedirectMiddleware
app.add_middleware(HTTPSRedirectMiddleware)
```

→ Render/Vercel/Cloudflare はデフォルトでHTTPS有効。

---

## 10. 依存ライブラリの脆弱性(=古い鍵)(15分)

### 10-1. 確認

```bash
pip install pip-audit
pip-audit
```

→ 既知の脆弱性のあるライブラリを警告。

### 10-2. 自動更新

GitHub の **Dependabot** を有効化:
- 設定 → Code security → Enable Dependabot alerts

→ 脆弱性発見時に自動 PR 作成。

> **なぜ重要?**
> 自分のコードがどんなに完璧でも、使ってるライブラリに脆弱性があれば**そこから侵入される**。
> 「ドアは新品でも、玄関の鍵が10年前の型」みたいな状態。

---

## 11. ロギングの注意(=日記の書き方)(10分)

### 11-1. NG

```python
# ❌ ログにパスワード残す
logger.info(f"Login attempt: {email}, {password}")

# ❌ 機密データをログに
logger.info(f"User data: {user.dict()}")  # ← password_hash含むかも
```

### 11-2. OK

```python
# ✅ 機密は除外
logger.info(f"Login attempt: {email}")

# ✅ ログ用 dict
def safe_user(user):
    return {"id": user.id, "email": user.email}  # passwordなし

logger.info(f"User: {safe_user(user)}")
```

> **罠:** ログファイルが漏洩したら、**全パスワード平文露出**。
> 本番ログは「**機密情報を絶対に書かない**」が鉄則。

---

## 12. CORS の罠(10分)

```python
# ❌ ワイルドカード許可
allow_origins=["*"]  # 誰でもアクセス可

# ✅ 信頼できるドメインだけ
allow_origins=["https://my-frontend.com"]
```

---

## 13. PII(個人情報)取扱(15分)

### 13-1. 法律

日本: **個人情報保護法**、EU: **GDPR**

### 13-2. 守ること

- 必要最小限のデータだけ取得
- 暗号化保存(可能なら)
- 削除請求に応える(忘れられる権利)
- 第三者提供時は明示・同意

### 13-3. Python での暗号化

```bash
pip install cryptography
```

```python
from cryptography.fernet import Fernet

key = Fernet.generate_key()
f = Fernet(key)

encrypted = f.encrypt(b"機密情報")
decrypted = f.decrypt(encrypted)
```

---

## 14. 本番チェックリスト(必須)

- [ ] DEBUG=False(本番)
- [ ] SECRET_KEY は環境変数経由
- [ ] HTTPS 強制
- [ ] CORS 制限
- [ ] パスワード bcrypt
- [ ] SQLi 対策(ORM or プレースホルダ)
- [ ] レート制限
- [ ] 機密ログに残さない
- [ ] 古いライブラリ更新
- [ ] バックアップ自動
- [ ] エラー監視(Sentry)
- [ ] 不要ポート閉じる(ファイアウォール)

---

## 15. ⭐ 既存アプリのセキュリティ監査(60分)

クロードに頼む:

```
Day 19 で作った FastAPI TODOアプリのセキュリティ監査をして。

チェック項目:
1. パスワード保存方法
2. SQLi 対策
3. JWT の SECRET_KEY 管理
4. CORS 設定
5. レート制限
6. ログ内容(機密混入)
7. .env のGit履歴
8. HTTPS 強制
9. エラーメッセージで内部情報漏れてないか
10. 認可(他人のTODOアクセス防止)

問題があれば修正案を出す。
```

→ **これで「セキュリティチェック実施済み」**ポートフォリオが出来る。

---

## 16. 今日学んだ用語まとめ

| 用語 | 一言で |
|---|---|
| OWASP Top 10 | 世界の最重要脆弱性10選 |
| bcrypt | 安全なパスワードハッシュ化 |
| SQLインジェクション | 文字列埋め込みでDB乗っ取り攻撃 |
| プレースホルダ | SQLi 防御の基本(`?` や `:name`) |
| XSS | 他人のブラウザでJS実行させる攻撃 |
| エスケープ | `<` を `&lt;` に変換してHTMLじゃなくする |
| CSRF | 他サイトから勝手なリクエスト送る攻撃 |
| .env | 機密を環境変数で管理するファイル |
| 認証(Authentication) | あなたは誰? |
| 認可(Authorization) | あなたは何できる? |
| レート制限 | 1分N回までに制限する仕組み |
| HTTPS | 通信を暗号化するプロトコル |
| Let's Encrypt | 無料SSL証明書 |
| pip-audit | 依存ライブラリ脆弱性スキャン |
| Dependabot | GitHubの自動依存更新 |
| Sentry | 本番エラー監視 |
| GDPR | EUの個人情報保護法 |
| Fernet | Pythonの対称暗号化ライブラリ |

---

## 💼 この章まで終わったら受けられる案件

### 受けられる案件(具体例)
- **セキュリティ監査・コードレビュー**:単価 30〜100万円(プラットフォーム例:ランサーズ「ビジネス」/直契約/コンサル)
- **既存Webアプリの脆弱性診断レポート**:単価 30〜80万円(OWASP Top 10 ベース)
- **認証・認可システムの再設計**:単価 30〜80万円(JWT / OAuth / RBAC 整備)

### クロードへの頼み方(営業文を書かせる例)
```
法人向けの「Webアプリ脆弱性診断 + 改修提案」サービスの DM 文を書いて。
Day 20 までで習得:OWASP Top 10 / SQLi 対策 / XSS / CSRF / bcrypt / レート制限 / 認証認可
売り:診断 + 改修 + 再診断までワンストップ
価格:診断のみ 20万円〜 / 改修込み 50万円〜
ターゲット:中小〜中堅のWebサービス運営会社
トーン:不安を煽らず、客観的なリスク説明と「直せば安心」のポジティブ訴求
500字
```

### この章だけでは足りないもの(次に学ぶべき)
- セキュリティ深掘り(Day 39)で Pen-test までできるレベルへ
- AWS のセキュリティ機能(IAM / WAF / GuardDuty)
- インシデント対応のフロー設計

---

## ✅ チェック

- [ ] OWASP Top 10 を概要理解
- [ ] パスワードを bcrypt でハッシュ
- [ ] SQLi 対策できる(プレースホルダ/ORM)
- [ ] XSS 防御理解
- [ ] 機密を .env で管理、Gitに上げない
- [ ] 認可と認証の違い理解
- [ ] レート制限実装できる
- [ ] HTTPS 必須を理解
- [ ] pip-audit で依存チェック
- [ ] Dependabot 有効化
- [ ] **既存アプリの監査チェックリスト通った**

---

## 進捗

```
Day 20: セキュリティ完了。OWASP Top 10/bcrypt/SQLi/CORS/レート制限/認可。
法人面談突破レベル。
```

## 次

`21_チーム開発.md` へ。**コードレビュー・アジャイル**。
