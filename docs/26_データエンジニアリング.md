# Day 26:データエンジニアリング - ETL・Airflow・dbt

## 💰 この章後の月収:**+30〜80万円**(データエンジニア案件)
## ⏱ 所要:3-4時間

データ分析の前段階「**データ集めて整える**」を担当。
中規模以上の企業で**月単価80-120万**の高単価領域。

---

## ゴール

- ETL の概念を理解
- Airflow / Prefect でワークフロー
- dbt でデータモデリング
- DWH(BigQuery/Snowflake)連携
- データレイク・データマート設計

---

## 1. ETL とは(15分)

```
Extract(抽出) → Transform(変換) → Load(投入)
```

例:
- E:CRMからAPIで顧客データ取得
- T:住所を統一フォーマットに変換、重複削除
- L:DWH(BigQuery)に投入

### ETL vs ELT

最近は **ELT**(先にRoadしてからDWH内でTransform)が主流。

---

## 2. Airflow - ワークフロー管理(60分)

「**毎朝6時にCSV取得 → 加工 → メール送信**」みたいなパイプラインを管理。

### インストール(Docker推奨)

```bash
mkdir airflow && cd airflow
curl -LfO 'https://airflow.apache.org/docs/apache-airflow/stable/docker-compose.yaml'
docker compose up
```

→ http://localhost:8080(admin/admin)

### DAG(有向非巡回グラフ)の書き方

`dags/etl_sales.py`:
```python
from datetime import datetime
from airflow import DAG
from airflow.operators.python import PythonOperator

def extract(**ctx):
    import requests
    data = requests.get("https://api.example.com/sales").json()
    ctx["ti"].xcom_push(key="raw", value=data)

def transform(**ctx):
    raw = ctx["ti"].xcom_pull(key="raw")
    cleaned = [r for r in raw if r["amount"] > 0]
    ctx["ti"].xcom_push(key="clean", value=cleaned)

def load(**ctx):
    clean = ctx["ti"].xcom_pull(key="clean")
    # DWH に挿入
    print(f"Loaded {len(clean)} rows")

with DAG(
    "sales_etl",
    start_date=datetime(2026, 1, 1),
    schedule_interval="0 6 * * *",  # 毎朝6時
    catchup=False,
    tags=["etl"],
) as dag:
    e = PythonOperator(task_id="extract", python_callable=extract)
    t = PythonOperator(task_id="transform", python_callable=transform)
    l = PythonOperator(task_id="load", python_callable=load)
    
    e >> t >> l
```

→ **毎朝自動実行・失敗通知・再実行**が標準装備。

---

## 3. Prefect(モダンな代替)(30分)

Airflow より**書きやすい**新興ツール。

```bash
pip install prefect
```

```python
from prefect import flow, task
import requests

@task(retries=3, retry_delay_seconds=10)
def extract():
    return requests.get("https://api.example.com/sales").json()

@task
def transform(data):
    return [r for r in data if r["amount"] > 0]

@task
def load(data):
    print(f"Loaded {len(data)} rows")

@flow
def etl_pipeline():
    raw = extract()
    cleaned = transform(raw)
    load(cleaned)

if __name__ == "__main__":
    etl_pipeline.serve(name="sales-etl", cron="0 6 * * *")
```

→ **Pythonコードそのまま**でワークフロー化。

---

## 4. dbt - SQLでデータモデリング(40分)

「**SQLで再利用可能なデータモデルを書く**」。データアナリストの主戦場。

### インストール

```bash
pip install dbt-postgres dbt-bigquery
```

### モデル定義 `models/orders_summary.sql`

```sql
{{ config(materialized='table') }}

SELECT
    DATE_TRUNC('month', created_at) as month,
    user_id,
    COUNT(*) as order_count,
    SUM(amount) as total_amount
FROM {{ ref('orders_raw') }}
WHERE status = 'completed'
GROUP BY 1, 2
```

### 実行

```bash
dbt run     # モデルをDWHに反映
dbt test    # データ品質チェック
dbt docs serve  # ドキュメント自動生成
```

→ **SQLを再利用・テスト・自動ドキュメント化**できる。

---

## 5. BigQuery 連携(20分)

```bash
pip install google-cloud-bigquery
```

```python
from google.cloud import bigquery

client = bigquery.Client(project="my-project")

# クエリ実行
df = client.query("SELECT * FROM dataset.orders LIMIT 100").to_dataframe()

# DataFrame → BigQuery
client.load_table_from_dataframe(df, "dataset.orders_backup").result()
```

→ pandas との往復が**ものすごく楽**。

---

## 6. Snowflake / Redshift も類似(10分)

```python
# Snowflake
import snowflake.connector

conn = snowflake.connector.connect(
    user="x", password="y", account="z",
    warehouse="MY_WH", database="MY_DB"
)
df = pd.read_sql("SELECT * FROM orders", conn)
```

→ **API ほぼ同じ**。覚えれば横展開可能。

---

## 7. データレイク(S3 + Parquet)(20分)

```python
import pandas as pd

df = pd.read_csv("data.csv")

# Parquet形式(列指向、超高速・圧縮)
df.to_parquet("data.parquet")

# S3 に直接保存(s3fs)
df.to_parquet("s3://my-bucket/data.parquet")

# 読み込み
df = pd.read_parquet("s3://my-bucket/data.parquet")
```

→ **TB級データ**は CSV ではなく Parquet。

---

## 8. Polars - pandas より100倍速い(20分)

```bash
pip install polars
```

```python
import polars as pl

df = pl.read_csv("huge.csv")
result = (
    df.filter(pl.col("amount") > 1000)
    .group_by("category")
    .agg(pl.col("amount").sum())
    .sort("amount", descending=True)
)
```

→ Rust製、**10億行でもサクサク**。新規案件はpolars検討。

---

## 9. データ品質テスト(20分)

### Great Expectations

```bash
pip install great-expectations
```

```python
import great_expectations as gx

context = gx.get_context()
ds = context.data_sources.add_pandas("my_data")
asset = ds.add_dataframe_asset("orders")

# 期待値
batch = asset.add_batch_definition_whole_dataframe("batch1").get_batch({"dataframe": df})
batch.validate(gx.expectations.ExpectColumnValuesToBeBetween(column="amount", min_value=0, max_value=1000000))
```

→ 「**金額が0以上**」「**emailの形式が正しい**」などをコードで保証。

---

## 10. ⭐ 売れる成果物 #12:データパイプライン(60分)

クロードに頼む:

```
以下を実装するデータパイプラインのプロジェクトを作って。

要件:
1. Source: REST API(JSON)からデータ取得(模擬データでOK)
2. Stage: PostgreSQL に raw データを保存
3. Transform: dbt で集計テーブル作成(月別・カテゴリ別)
4. Orchestrate: Prefect で毎朝6時実行、失敗時Slack通知
5. Test: Great Expectations でデータ品質チェック
6. Output: ダッシュボード(Streamlit + Plotly)

Docker Compose で全部起動できる構成。
README に「docker compose up で全部動く」と明記。
```

→ **これでデータエンジニア名乗れる**。月単価80万圏。

---

## ✅ チェック

- [ ] ETL/ELT の違い理解
- [ ] Airflow or Prefect で DAG/Flow 書ける
- [ ] dbt で SQL モデル書ける
- [ ] BigQuery / Snowflake を Python から触れる
- [ ] Parquet / S3 でデータレイク構築
- [ ] Polars 試した
- [ ] Great Expectations でデータ品質
- [ ] **データパイプライン プロジェクト完成**

---

## 進捗

```
Day 26: データエンジニアリング完了。
Airflow/Prefect/dbt/BigQuery/Polars。
データエンジニア案件(月80万圏)対応。
```

## 次

`27_ML_MLOps.md` へ。**機械学習実装**。
