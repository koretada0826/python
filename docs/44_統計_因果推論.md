# Day 44:統計学・Bayesian・因果推論・時系列

## 💡 これは何?(小学生でもわかる説明)

****まぐれかどうかを見分ける**数学**

### なぜ大事?
A/Bテスト分析・因果推論で月単価100万円圏

### イメージ
コイン10回連続で表 = **まぐれ?それとも秘密がある?**

### Claude Code に何を頼めばいいか
この章で覚えるのは「**何ができるか**」と「**何を頼めばいいか**」だけでOK。
具体的なコードは Claude Code に書かせる。

---

## 💰 +30-100万(データサイエンティスト)
## ⏱ 25時間

「**A/Bテスト結果が有意か?**」「**売上が増えた本当の原因は?**」を**数学で**答える力。

---

## 小学生説明
**まぐれかどうか**を見分ける数学。「コイン10回投げて全部表 = まぐれ?」を計算で。

---

## 1. 統計検定(必修)(30分)

### t検定(2群の平均差)

```python
from scipy import stats
group_a = [100, 110, 95, ...]  # コントロール
group_b = [120, 115, 130, ...] # 介入

t, p = stats.ttest_ind(group_a, group_b)
if p < 0.05:
    print("有意差あり")
```

### カイ二乗検定(クロス集計)

```python
table = [[100, 50], [80, 70]]  # CV / 非CV
chi2, p, dof, expected = stats.chi2_contingency(table)
```

### A/Bテスト

```python
# CTR比較
def ab_test(visits_a, clicks_a, visits_b, clicks_b):
    ctr_a = clicks_a / visits_a
    ctr_b = clicks_b / visits_b
    pooled = (clicks_a + clicks_b) / (visits_a + visits_b)
    se = sqrt(pooled * (1-pooled) * (1/visits_a + 1/visits_b))
    z = (ctr_b - ctr_a) / se
    p = 2 * (1 - stats.norm.cdf(abs(z)))
    return p
```

→ **本当に効果あったか**を統計で判定。

---

## 2. ベイズ統計(30分)

### PyMC

```bash
pip install pymc
```

```python
import pymc as pm

with pm.Model() as model:
    p_a = pm.Beta("p_a", alpha=1, beta=1)
    p_b = pm.Beta("p_b", alpha=1, beta=1)
    
    obs_a = pm.Binomial("obs_a", n=visits_a, p=p_a, observed=clicks_a)
    obs_b = pm.Binomial("obs_b", n=visits_b, p=p_b, observed=clicks_b)
    
    diff = pm.Deterministic("diff", p_b - p_a)
    
    trace = pm.sample(2000)

# 「Bが Aより良い確率」が直接出る
prob_b_better = (trace.posterior["diff"] > 0).mean()
```

→ p値より直感的。**「90%の確率でBが勝つ」**と説明できる。

---

## 3. 因果推論(40分)

### 相関 ≠ 因果

「アイス売上UPと水死者UP」 → 相関ある、原因は両方とも気温。

### 介入(因果効果を測る)

#### 1. RCT(ランダム化比較試験)
ユーザーをランダムに2群に分けて、片方だけ介入。

#### 2. DiD(差分の差分法)

```python
# Before/After × Treatment/Control
import statsmodels.formula.api as smf

model = smf.ols("y ~ time + treatment + time:treatment", data=df).fit()
print(model.summary())
# time:treatment の係数が因果効果
```

#### 3. 傾向スコアマッチング

```bash
pip install causalml
```

```python
from causalml.inference.meta import LRSRegressor
treatment_effect = LRSRegressor().estimate_ate(X, treatment, y)
```

#### 4. Instrumental Variable(操作変数)

→ 観察データから因果を推定。

---

## 4. 時系列予測(40分)

### Prophet(Facebook)

```bash
pip install prophet
```

```python
from prophet import Prophet

df = pd.DataFrame({"ds": dates, "y": values})
m = Prophet(yearly_seasonality=True, weekly_seasonality=True)
m.fit(df)

future = m.make_future_dataframe(periods=30)
forecast = m.predict(future)
m.plot(forecast)
```

→ **売上予測・需要予測**が1分で。

### ARIMA / SARIMA

```python
from statsmodels.tsa.arima.model import ARIMA
model = ARIMA(series, order=(1,1,1))
fit = model.fit()
forecast = fit.forecast(steps=30)
```

### LSTM(深層学習)

PyTorch で時系列モデル。

---

## 5. 強化学習基礎(20分)

```bash
pip install stable-baselines3 gymnasium
```

```python
import gymnasium as gym
from stable_baselines3 import PPO

env = gym.make("CartPole-v1")
model = PPO("MlpPolicy", env, verbose=1)
model.learn(total_timesteps=10000)
```

→ ゲーム・トレーディング・ロボット制御。

---

## 6. ⭐ 課題

```
1. ECサイトの過去A/Bテストデータを t検定 / Bayesian で再分析
2. 売上の前年比を Prophet で予測 → 経営報告書
3. 因果推論で「キャンペーン効果」を傾向スコアで測定
4. LSTM で BTC 価格予測(過去データ使って)
```

---

## ✅ チェック
- [ ] t検定 / カイ二乗 書ける
- [ ] PyMC でベイズ
- [ ] Prophet で時系列予測
- [ ] DiD / 傾向スコア理解
- [ ] 強化学習の概念

## 次
`45_ベクトルDB_RAG高度.md`
